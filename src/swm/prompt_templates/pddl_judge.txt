Judge whether the Candidate plan is physically executable and fully satisfies the Instruction.

# Task

Instruction:
{instruction}

Scene image: attached.

# Candidate Initial State

{candidate_initial_state}

Candidate PDDL is unverified.

# Candidate Goal

{candidate_goal}

# Programmatic Findings

{programmatic_findings}

# Evaluated Symbolic Trace

{evaluated_symbolic_trace}

# Reference

Keyframe-based action sequence (evidence, never Candidate actions):
{kf_actions}

# Required Checks

Pass only if all three layers below succeed.

1. Instruction completion
- Build the complete contract: objects, quantities, source, destination, qualifiers, method, order, relations, cleanup, and persistence.
- Candidate history and result must establish every requirement. A missing transition or underspecified destination/relation is failure, not unknown.
- An imperative operation must occur in Candidate history; initial facts cannot perform it. Only stative wording such as "leave" or "end up" may already hold when scene evidence confirms it.
- Preserve stated order. Preparatory access may occur early, but each named completion before "then" or in a process list must finish before the later stage. Results commute only when neither creates clearance, load, receiver, or alignment for the other.
- Resolve ambiguous modifiers using the image and reference. Do not invent unsupported relations.
- A few singular symbols do not represent unmodeled remaining items unless explicitly defined as collections.

2. Symbolic execution
- Simulate only the Candidate trace, in order, from the Candidate Initial State. Never copy, assume, or execute actions from the reference plan.
- Every `Before` must hold initially or follow from an earlier Candidate change. Track bindings, possession, direct location, relations, and device state; support, containment, and possession are not interchangeable or transitive.
- `Before` lists claimed conditions. `State change` is only the claimed delta: `not` requires absence, `+` adds, and `-` removes. Candidate Goal and PDDL facts are claims to verify, not proof.

3. Physical execution
- Check every transition against the scene. Candidate predicates cannot make a blocked object, closed container, occupied destination, wrong alignment, possession conflict, or false source valid.
- For visible initial identity, possession, and location, clear image evidence outranks reference wording, which outranks Candidate claims. A missed small object, ambiguous adjacent boundary, or guessed reach is not contradiction. Matching reference source/destination corroborates it; hand choice alone does not prove exclusivity.
- Reference opening, clearance, reposition, transfer, or stopping before later use is causal evidence. Require an equivalent Candidate state. Alignment with one outlet, slot, or workspace does not align another.
- Track mixture, emptied containers, moved supports, geometry, and active processes. Do not infer impossible orientation or reach from category alone. Stop task-started processes unless continued operation is required.

# Decision

The reference is one witness, not mandatory wording or order. Accept alternatives proving the same contract. Reject the earliest decisive failure. Unknown applies only to unobservable incidental facts, not explicit requirements or unsupported preconditions.

# Output

Return exactly this JSON object and no additional fields:

{{
  "reasoning": "state the contract, then give the decisive completion, symbolic, or physical evidence",
  "pass": true or false,
  "feedback": "empty if pass=true; otherwise identify the erroneous step or unmet requirement"
}}
